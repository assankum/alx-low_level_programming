#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length to a string
 * @c: string to check its length
 * Return: length of string
 */

int _strlen(char *c)
{
	return (strlen(*c));
}
